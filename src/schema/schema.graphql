type Query {
  getNumbers: Game
}

type Game {
  id: String!
  orange: Int!
  blue: Int!
}

type Mutation {
  updateGame(id: String!, blue: Int!, orange: Int!): Game!
}

type Subscription {
  gameUpdated: Game
}